# API Server environment variables (copy to .env and fill in values)
# For Fly.io deployment, set these via `fly secrets set KEY=VALUE`

# OPENAI_API_KEY=sk-proj-...   # Required for /api/tts endpoint
# JWT_SECRET=your-jwt-secret
# REDIS_URL=redis://localhost:6379
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# URL of your frontend (for email links)
FRONTEND_URL=https://www.engg.online
# SMTP config for noreply@engg.online (Hostinger)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=noreply@engg.online
SMTP_PASS=lj*m8OHsZ7o?
SMTP_SECURE=true
